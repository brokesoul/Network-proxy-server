# Proxy Server – Test Artifacts

This folder contains manual test cases and their expected outputs
used to verify the correctness of the HTTP/HTTPS proxy server.

Each test file includes:
- Command to run
- What is being tested
- Expected output (terminal + behavior)

All tests were executed using `curl` via the proxy at `localhost:8888`.

